USB

Maybe I should make a better name for this
This is a program that finds every USB device attached to your computer and outputs some information about it.
That's all it does, I swear

How to build/run:
1) Make a bin/ directory
2) Run `make`
3) Run `bin/usb`
